{
  "name": "masa_dashboard",
  "version": "1.0.0",
  "description": "Monitor avionics data in real time",
  "main": "index.js",
  "scripts": {
    "prestart": "cd client && npm install && npm run build && cd ..",
    "start": "node index.js"
  },
  "dependencies": {
    "chokidar": "^2.1.2",
    "express": "^4.16.4",
    "papaparse": "^4.6.3",
    "socket.io": "^2.2.0"
  }
}
