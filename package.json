{
  "name": "masa_dashboard",
  "version": "1.0.0",
  "description": "Monitor avionics data in real time",
  "main": "index.js",
  "scripts": {
    "prestart": "cd client && npm install && npm run build && cd ..",
    "start": "node index.js"
  },
  "dependencies": {
    "chokidar": "^2.0.4",
    "express": "^4.16.3",
    "papaparse": "^4.6.0",
    "socket.io": "^2.0.3"
  }
}
