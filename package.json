{
  "name": "masa_dashboard",
  "version": "1.0.0",
  "description": "Monitor avionics data in real time",
  "main": "index.js",
  "scripts": {
    "prestart": "cd client && npm install && npm run build && cd ..",
    "start": "node index.js"
  },
  "dependencies": {
    "chokidar": "^3.0.2",
    "express": "^4.17.1",
    "papaparse": "^5.0.0",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {}
}
