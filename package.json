{
  "name": "masa_dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Monitor avionics data in real time",
  "main": "index.js",
  "scripts": {
    "prestart": "cd client && npm install && npm run build && cd ..",
    "start": "node index.js"
  },
  "dependencies": {
    "chokidar": "^3.3.1",
    "express": "^4.17.1",
    "papaparse": "^5.1.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {}
}
